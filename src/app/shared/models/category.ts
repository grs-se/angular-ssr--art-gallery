export interface ArtworkCategory {
  id: number;
  name: string;
  description?: string;
  shortDescription?: string;
  startDate?: string;
  endDate?: string;
  imageUrl?: string;
  count?: number;
}
